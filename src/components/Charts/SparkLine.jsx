import React from 'react'
import { SparklineComponent, Inject, SparklineTooltip } from '@syncfusion/ej2-react-charts'

function SparkLine({ currentColor, id, type, height, width, data }) {
  return (
    <SparklineComponent>
      
    </SparklineComponent>
  )
}

export default SparkLine